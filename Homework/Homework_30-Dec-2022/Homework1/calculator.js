b = prompt("Type the first numbers");
c = prompt("Type the second numbers");

var b = Number(b)
var c = Number(c)




    var plus = (b + c);
    alert(plus +" (plus calculation)");

    var minus = (b - c);
    alert(minus +" (minus calculation)");

    var multiply = (b * c);
    alert(multiply +" (multiply calculation)");

    var divide = (b / c +" (divide calculation)");
    alert(divide);

    var modulo = (b % c +" (modulo calculation)");
    alert(modulo);


